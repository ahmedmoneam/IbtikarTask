<resources>

    <!-- Base mApplication theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <!--<item name="colorControlHighlight">@color/colorAccent</item>-->
    </style>

    <style name="TextLabel" parent="Widget.Design.TextInputLayout">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/colorAccent</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="textInputLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/form_margin_left</item>
        <item name="android:layout_marginRight">@dimen/form_margin_right</item>
        <item name="android:layout_marginTop">@dimen/form_margin_top</item>
        <item name="android:theme">@style/TextLabel</item>
    </style>

    <style name="editText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:drawablePadding">8dp</item>
    </style>

    <style name="emailEditText" parent="editText">
        <item name="android:drawableLeft">@drawable/ic_mail</item>
        <item name="android:drawableStart">@drawable/ic_mail</item>
        <item name="android:imeOptions">actionNext</item>
        <item name="android:inputType">textEmailAddress</item>
        <item name="android:hint">@string/prompt_email</item>
    </style>

    <style name="passwordEditText" parent="editText">
        <item name="android:drawableLeft">@drawable/ic_lock</item>
        <item name="android:drawableStart">@drawable/ic_lock</item>
        <item name="android:inputType">textPassword</item>
    </style>

    <style name="nameEditText" parent="editText">
        <item name="android:drawableLeft">@drawable/ic_account</item>
        <item name="android:drawableStart">@drawable/ic_account</item>
        <item name="android:inputType">text</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/dim_gray</item>
        <item name="colorControlActivated">@color/colorPrimaryDark</item>
    </style>

    <style name="TempDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorPrimary</item>
    </style>

    <style name="progressBarStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="progressBarStyleWhite" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
